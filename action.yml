name: setup-cndi
description: Setup cndi CLI on GitHub Actions runners
inputs:
  version:
    description: Version of CNDI CLI to install
    required: false
    default: latest
runs:
  using: node16
  main: dist/index.js
